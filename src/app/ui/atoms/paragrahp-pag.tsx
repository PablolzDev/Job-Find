import React from 'react'


interface IpageProps {
    data: string;
    pagination: string;
}

export default function PageText({data, pagination}: IpageProps) {
  return (
    <div>PageText</div>
  )
}
